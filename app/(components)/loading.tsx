import { LoadingSpinner } from '../reusableComponents/ui/LoadingSpinner';

export default function Loading() {
    return <LoadingSpinner message="Loading page..." submessage="This will just take a moment" />;
}

